{% extends 'base.html.twig' %}

{% block body %}
    <h1> Hello {{ userName }}</h1>


    <a href="{{ path('app_login') }}">
        <span> Login </span>
    </a>

    <a href="{{ path('app_register') }}">
        <span> Register </span>
    </a>

{% endblock %}

