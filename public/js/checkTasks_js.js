/* var $container = $('.js-check-task');
$container.find('a').on('click', function (e){
    e.preventDefault();
    var $link = $(e.currentTarget);
    $.ajax({
        url: '/tasks/' + $link.data('') + '/check-done',
        method: 'POST'
    })
})*/